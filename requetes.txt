

CREATE TABLE outils(
	id TEXT PRIMARY KEY,
	name TEXT NOT NULL,
	description TEXT,
	quantity INT
		NOT NULL,
	available TINYINT 
		DEFAULT 1,
	broken TINYINT
		DEFAULT 0,
	must_command TINYINT
		DEFAULT 0
);
	
INSERT INTO outils (id, name, description, quantity) VALUES ('O1', 'marteau', 'Outils servant a planter des clous.....', 100);

CREATE TABLE bruts(
	id TEXT PRIMARY KEY,
	name TEXT NOT NULL,
	description TEXT,
	quantity INT
		NOT NULL,
	available TINYINT 
		DEFAULT 1,
	must_command TINYINT
		DEFAULT 0
);

INSERT INTO bruts (id, name, description, quantity)
	VALUES ('B1', 'planche 2x4', 'Planche de chene de 2x4x10', 10);

CREATE TABLE membres(
	id TEXT PRIMARY KEY,
	name TEXT NOT NULL,
	last_name TEXT NOT NULL,
	image_path TEXT,
	is_admin TINYINT 
		DEFAULT 0
);

INSERT INTO membres (id, name, last_name, image_path, is_admin)
	VALUES ('1245278', 'Daniel-Junior', 'Dubé', 'src/images/etudiants/1245278', 1);
INSERT INTO membres (id, name, last_name, image_path, is_admin)
	VALUES ('1357059', 'Sarah', 'Laflamme', 'src/images/etudiants/1357059', 0);

CREATE TABLE locations(
	item_id TEXT NOT NULL,
	owner_id TEXT NOT NULL,
	admin_id TEXT NOT NULL,
	date DATE
		DEFAULT (datetime('now','localtime')),
	returned TINYINT
		DEFAULT 0
);

INSERT INTO locations (item_id, owner_id, admin_id)
	VALUES ('1', '1245278', '1111111');

CREATE TABLE dons(
	item_id TEXT NOT NULL,
	owner_id TEXT NOT NULL,
	admin_id TEXT NOT NULL,
	date DATE
		DEFAULT (datetime('now','localtime'))
);

INSERT INTO dons(item_id, owner_id, admin_id)
	VALUES ('1', '1245278', '1111111');
